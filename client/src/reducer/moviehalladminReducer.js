import * as actionType from '../actions/ActionType';

const defaultState = {

}

export function moviehalladminReducer(state = defaultState, action){

  const newState = {...state};
  return newState;

}
